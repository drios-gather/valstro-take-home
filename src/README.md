# Valstro Take-Home Assessment

### Setup
```
npm install
```

### Run
From the repo root (e.g. `.../valstro-assessment/`):

**Using tsx or ts-node**
```
npx <tsx/ts-node> src/index.ts
```

Run alongside the prepared `aarronbate/socketio-backend` Docker image.
